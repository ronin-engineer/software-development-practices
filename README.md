# Ronin Engineer / 01. Software Development

## How To Run

- Set up a Postgres server (using docker compose)
- Create schema using script in `sql/schema.sql`
- Edit configurations in `src/main/resources/application.yaml`

